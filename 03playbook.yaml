- name: variable
  hosts: localhost
  vars:
    trainer: "mani"
    course: "devops"
  tasks:
  - name: check variables 
    ansible.builtin.debug:
      msg: "hi iam learning {{course}}, trainer is {{trainer}}"