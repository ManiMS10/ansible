- name: variables
  hosts: localhost
  vars:
    course: "devops"
    trainer: "mani"
  tasks:
  - name: check variables 
    ansible.builtin.debug:
      msg: "hi, i am learning {{course}}, trainer is {{trainer}}"
