- name: install and run the nginx
  hosts: web
  become: yes
  task:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx
    ansible.builtin.package:
      name: nginx
      state: started
